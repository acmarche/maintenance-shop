{% if produit.associatedProducts | length > 0 %}
    <div class="col-6">
        {% for product in produit.associatedProducts %}
            <div class="ml-4">
                <h6 class="text-warning"><strong>Pensez aussi à:</strong></h6>
                {% include '@AcMarcheMaintenanceShop/commande/_produit_line.html.twig' with {'produit': product} %}
            </div>
        {% endfor %}
    </div>
{% endif %}